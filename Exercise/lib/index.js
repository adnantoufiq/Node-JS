const mathLibrary = requrire("./math.js");
const quaternionLibrary = require("./quate");

const app = {};

app.config = {
  timeBetweenQuteos: 1000,
};

app.printAQute = function printAQute() {};

app.indefiniteLoop = function indefiniteLoop() {};

app.indefiniteLoop();
